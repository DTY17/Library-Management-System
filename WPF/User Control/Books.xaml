<UserControl x:Class="WPF.User_Control.Books"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="510" d:DesignWidth="700">

    <UserControl.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                Background="White"
                CornerRadius="8"
                BorderBrush="#E0E7FF"
                BorderThickness="2"
                Padding="12,8">

                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"/>

                                <TextBlock x:Name="Placeholder"
                           Text="{TemplateBinding Tag}"
                           Foreground="Gray"
                           Margin="5,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Opacity="0.6"
                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>

                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#1F2937"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="20,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4F46E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#EF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#1F2937"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="316"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="White" 
                Margin="20,20,20,10" 
                CornerRadius="16" 
                Padding="30,20">
            <Border.Effect>
                <DropShadowEffect Color="#E5E7EB" BlurRadius="15" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Book Management" 
                           Grid.Row="0" Grid.ColumnSpan="3"
                           FontSize="20" FontWeight="Bold"
                           Foreground="#1F2937"
                           Margin="0,0,0,15"/>

                <TextBlock Text="Book Title" Grid.Row="1" Grid.Column="0" Margin="0,0,20,0" Foreground="#4B5563" VerticalAlignment="Center"/>
                <TextBlock Text="Author" Grid.Row="2" Grid.Column="0" Margin="0,0,20,0" Foreground="#4B5563" VerticalAlignment="Center"/>
                <TextBlock Text="Genre" Grid.Row="3" Grid.Column="0" Margin="0,0,20,0" Foreground="#4B5563" VerticalAlignment="Center"/>
                <TextBlock Text="Available" Grid.Row="4" Grid.Column="0" Margin="0,0,20,0" Foreground="#4B5563" VerticalAlignment="Center"/>

                <!-- TextBoxes -->
                <TextBox x:Name="BookBookInput" Style="{StaticResource ModernTextBox}" Tag="Book" Grid.Row="1" Grid.Column="1" Margin="0,5"/>
                <TextBox x:Name="BookAuhtorInput" Style="{StaticResource ModernTextBox}" Tag="Author" Grid.Row="2" Grid.Column="1" Margin="0,5"/>
                <TextBox x:Name="BookGenreInput" Style="{StaticResource ModernTextBox}" Tag="Genre" Grid.Row="3" Grid.Column="1" Margin="0,5"/>
                <TextBox x:Name="BookAvailableInput" Style="{StaticResource ModernTextBox}" Tag="Available" Grid.Row="4" Grid.Column="1" Margin="0,5"/>

                <Button x:Name="AddBookBtn" Content="Add Book" Style="{StaticResource ModernButton}"
                        Grid.Row="1" Grid.Column="2" Width="110" Height="38" Margin="15,5,0,5" Click="AddBookBtn_Click"/>

                <Button x:Name="UpdateBookBtn" Content="Update" Style="{StaticResource ModernButton}"
                        Grid.Row="2" Grid.Column="2" Width="110" Height="38" Margin="15,5,0,5" Click="UpdateBookBtn_Click"/>

                <Button x:Name="DeleteBookBtn" Content="Delete" Style="{StaticResource DeleteButton}"
                        Grid.Row="3" Grid.Column="2" Width="110" Height="38" Margin="15,5,0,5" Click="DeleteBookBtn_Click"/>

                <Button x:Name="CancelBookBtn" Content="Cancel" Style="{StaticResource DeleteButton}"
                        Grid.Row="4" Grid.Column="2" Width="110" Height="38" Margin="15,5,0,5" Click="CancelBookBtn_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" Margin="20,10,20,20" CornerRadius="16">
            <Border.Effect>
                <DropShadowEffect Color="#E5E7EB" BlurRadius="15" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>

            <DataGrid x:Name="BooksTable"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      Margin="1" PreviewMouseLeftButtonDown="ClickRowTable">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Book Title" Binding="{Binding Book}" Width="*"/>
                    <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*"/>
                    <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="120"/>
                    <DataGridTextColumn Header="Available" Binding="{Binding Available}" Width="100"/>
                </DataGrid.Columns>

            </DataGrid>
        </Border>

    </Grid>

</UserControl>
